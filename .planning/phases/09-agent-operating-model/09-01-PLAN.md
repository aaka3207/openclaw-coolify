---
phase: 09-agent-operating-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SOUL.md
  - TOOLS.md
  - AGENTS.md
  - docs/reference/templates/HEARTBEAT.md
autonomous: true

must_haves:
  truths:
    - "SOUL.md has a dedicated 'n8n Boundary' section stating agent is the judgment layer, n8n handles pipeline plumbing"
    - "SOUL.md Memory Architecture section no longer references QMD or NOVA Memory (superseded)"
    - "TOOLS.md n8n section opens with a role boundary note — 'Ameer builds n8n workflows, you do not'"
    - "AGENTS.md ends with an 'Operator Domain' section defining agent vs operator ownership layers"
    - "docs/reference/templates/HEARTBEAT.md contains a correct periodic check checklist (inbox, calendar, leads)"
  artifacts:
    - path: "SOUL.md"
      provides: "n8n Boundary section before ACIP block, updated Memory Architecture"
      contains: "n8n Boundary"
    - path: "TOOLS.md"
      provides: "Role boundary note at top of n8n section"
      contains: "Ameer builds"
    - path: "AGENTS.md"
      provides: "Operator Domain section appended after Memory Search Protocol"
      contains: "Operator Domain"
    - path: "docs/reference/templates/HEARTBEAT.md"
      provides: "Correct periodic check template"
      contains: "Periodic Checks"
  key_links:
    - from: "SOUL.md n8n Boundary"
      to: "TOOLS.md role boundary note"
      via: "consistent message: n8n calls agent, agent does not build/manage n8n workflows"
      pattern: "Ameer builds"
    - from: "AGENTS.md Operator Domain"
      to: "SOUL.md n8n Boundary"
      via: "same locked decision expressed in both agent-facing and operator-facing docs"
      pattern: "Operator Domain"
---

<objective>
Encode the correct agent operating model into repo behavioral config files. The agent self-organized after Phase 8 in ways that violated the intended boundary: it built n8n workflow JSON files and spawned recurring cron sub-agents for data pipelines. These behavioral files are the authoritative spec — once updated and seeded, they constrain future drift.

Purpose: Fix behavioral gaps before the operating model drifts further. Locked decisions require explicit prose encoding in SOUL.md, TOOLS.md, AGENTS.md, and the HEARTBEAT.md template.
Output: Four updated files with clear n8n boundary, Operator Domain, and correct heartbeat checklist.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-agent-operating-model/09-CONTEXT.md
@.planning/phases/09-agent-operating-model/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add n8n Boundary section to SOUL.md and update stale Memory Architecture</name>
  <files>SOUL.md</files>
  <action>
Read SOUL.md in full first. Then make two targeted edits:

**Edit 1 — Replace the "Memory Architecture" section (lines 267-283):**
Replace the entire block from "🧠 Memory Architecture" through the closing "⸻" (line 284) with:

```
🧠 Memory Architecture

The main agent uses two memory tiers:

**Operational (permanent):**
- `memory/YYYY-MM-DD.md` — daily narrative logs, kept forever as audit trail
- `memory/digests/YYYY-WXX.md` — weekly digests produced by compaction cron
- `MEMORY.md` — curated long-term memory (main session only)

**Transaction (30-day rotation):**
- `leads/` — lead screening pipeline output
- `monitor.log` — sandbox health monitoring log
- `recovery.log` — sandbox recovery log

Weekly compaction runs via OpenClaw cron (Sunday 6 AM): reads past week's daily logs, distills to `memory/digests/YYYY-WXX.md`, leaves daily logs intact.

⸻
```

**Edit 2 — Insert "n8n Boundary" section BEFORE the ACIP block:**
The ACIP block starts at the line `<!-- ACIP:BEGIN clawdbot SECURITY.md -->` (currently line 386). Insert the following block immediately before that line (after the "🏁 Final Mental Model" section's closing "⸻"):

```

⸻

🔌 n8n Boundary — Your Role in the Pipeline

You are the judgment layer. n8n handles scheduling, API calls, and data pipeline plumbing. You handle classification, decisions, and structured output production.

**Correct:** n8n calls you → you return output → n8n handles the result
**Wrong:** You spawn a cron sub-agent to poll external APIs
**Wrong:** You build or manage n8n workflow JSON files
**Wrong:** You orchestrate recurring sub-agents for data collection

You CAN use OpenClaw's native cron for internal maintenance:
- Weekly memory compaction (distill daily logs to digest)
- Heartbeat checks and inbox monitoring

You CANNOT:
- Create, update, or activate n8n workflows
- Build cron pipelines that substitute for n8n workflows
- Spawn sub-agents to run recurring external data fetches

If you identify a pipeline need, note it for Ameer. He builds n8n workflows.

⸻

```

Do NOT touch the ACIP block (`<!-- ACIP:BEGIN ... -->` through `<!-- ACIP:END -->`). New content goes before it.
  </action>
  <verify>
Run: `grep -n "n8n Boundary" SOUL.md` — must find the section.
Run: `grep -n "QMD\|NOVA Memory\|nova-memory" SOUL.md` — must return nothing (stale refs removed).
Run: `grep -n "ACIP:BEGIN" SOUL.md` — must still find the ACIP block (not deleted).
  </verify>
  <done>
SOUL.md contains "n8n Boundary" section before ACIP block. QMD/NOVA references are gone from Memory Architecture. ACIP block is intact. Memory Architecture reflects current two-tier file-based model.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add role boundary note to TOOLS.md and add Operator Domain section to AGENTS.md</name>
  <files>TOOLS.md, AGENTS.md</files>
  <action>
**TOOLS.md edit:**

Read TOOLS.md in full. The n8n section starts at line 7 with "## n8n Workflow Automation". Insert a role boundary note as the first paragraph under that heading, before the existing "You have the n8n-manager skill..." paragraph:

```markdown
> **Role boundary:** Ameer builds and manages n8n workflows. You do NOT create, update, activate, or repair n8n workflows. You CAN query workflow status and executions to diagnose issues and then surface findings to Ameer. You CAN use the n8n API to check execution logs when diagnosing a failure. If you identify a pipeline need, document it and tell Ameer — you do not build it.
```

Place this block immediately after the `## n8n Workflow Automation` heading line (line 7), as a blockquote before the existing "You have the n8n-manager skill..." paragraph.

Also update the Escalation section (last paragraph, currently says "For everything else — workflow repairs, new automations, capability building — handle autonomously using the n8n API directly.") to instead say:

```markdown
For everything else — credential needs, infrastructure changes, architectural decisions, new n8n workflows — escalate to Ameer. Do not attempt to build or repair n8n workflows autonomously.
```

---

**AGENTS.md edit:**

Read AGENTS.md in full. The file ends at line 281 with the Memory Search Protocol section. After line 281, append the following new section:

```markdown

---

## Operator Domain

The system has two ownership layers:

**Agent layer (yours):**
- Judgment, classification, decisions, structured output production
- Memory files in your workspace (`memory/`, `MEMORY.md`, `leads/`)
- Internal cron for maintenance tasks (memory compaction, heartbeat checks)
- Sub-agents for bounded, one-shot tasks (classify this batch, summarize these docs)

**Operator layer (Ameer's):**
- All n8n workflow creation and management
- Dockerfile, bootstrap.sh, docker-compose.yaml
- Infrastructure-level cron jobs
- New agent registrations

When you identify a pipeline need — a new data source, a new automation — document it and surface it to Ameer. You do not build it. n8n is Ameer's tool; you are called by n8n, not the other way around for data pipelines.

**Sub-agent guidance:**
- Use sub-agents for bounded, one-shot tasks
- Do NOT spawn recurring cron sub-agents for external data collection
- Sub-agent output returns to you; you synthesize and act on it

**When to escalate vs. act:**
- Escalate: new n8n workflow needed, infrastructure change, new API credential, new agent registration
- Act autonomously: memory reads/writes, single-session research, classification tasks, drafting
```
  </action>
  <verify>
Run: `grep -n "Role boundary" TOOLS.md` — must find the blockquote.
Run: `grep -n "handle autonomously using the n8n API" TOOLS.md` — must return nothing (replaced).
Run: `grep -n "Operator Domain" AGENTS.md` — must find the section.
Run: `wc -l AGENTS.md` — must be greater than 281 (new content appended).
  </verify>
  <done>
TOOLS.md n8n section opens with role boundary blockquote. TOOLS.md escalation section no longer instructs autonomous workflow repairs. AGENTS.md has "Operator Domain" section appended after Memory Search Protocol.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite HEARTBEAT.md template with correct periodic check checklist</name>
  <files>docs/reference/templates/HEARTBEAT.md</files>
  <action>
Read the current docs/reference/templates/HEARTBEAT.md. It is nearly empty — just a comment saying to leave it empty to skip heartbeat calls.

Rewrite the file with a correct periodic check template. The template should reflect the correct operating model (inbox/calendar/leads checks) without pipeline health monitoring. Use the YAML frontmatter from the existing file, then replace the body:

```markdown
---
summary: "Workspace template for HEARTBEAT.md"
read_when:
  - Bootstrapping a workspace manually
---
# HEARTBEAT.md

## Periodic Checks (run 2-4x daily, rotate through)

- **Emails** — Any urgent unread messages?
- **Calendar** — Events in the next 24-48h?
- **Leads** — Any new leads to review in `leads/`?

## Memory Hygiene (weekly, during low-activity heartbeat)

- Skim recent daily logs — anything worth adding to `MEMORY.md`?
- Leads older than 30 days in `leads/` — clean up

## When to Reply vs HEARTBEAT_OK

**Reply when:**
- Urgent email arrived
- Calendar event under 2 hours away
- Lead requires a human decision
- It has been more than 8 hours since the last message

**Stay quiet (HEARTBEAT_OK) when:**
- Late night (23:00-08:00) unless urgent
- Nothing new since last check
- You checked less than 30 minutes ago
```

Do not monitor n8n pipeline health in the heartbeat — the n8n Error Handler workflow (Phase 8) handles pipeline failures automatically.
  </action>
  <verify>
Run: `grep -n "Periodic Checks" docs/reference/templates/HEARTBEAT.md` — must find the section.
Run: `grep -n "n8n\|pipeline\|workflow" docs/reference/templates/HEARTBEAT.md` — must return nothing (no pipeline monitoring).
Run: `grep -n "HEARTBEAT_OK" docs/reference/templates/HEARTBEAT.md` — must find the quiet signal.
  </verify>
  <done>
docs/reference/templates/HEARTBEAT.md contains inbox/calendar/leads check checklist. No n8n pipeline monitoring. HEARTBEAT_OK quiet conditions are documented.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -n "n8n Boundary" SOUL.md` — section exists before ACIP block
2. `grep -n "ACIP:BEGIN" SOUL.md` — ACIP block still present
3. `grep -n "QMD\|nova-memory\|NOVA Memory" SOUL.md` — returns nothing
4. `grep -n "Role boundary" TOOLS.md` — role boundary note exists in n8n section
5. `grep -n "handle autonomously using the n8n API" TOOLS.md` — returns nothing (removed)
6. `grep -n "Operator Domain" AGENTS.md` — section exists
7. `grep -n "Periodic Checks" docs/reference/templates/HEARTBEAT.md` — exists
8. `grep -rn "QMD\|nova-memory" SOUL.md TOOLS.md AGENTS.md` — no stale refs
</verification>

<success_criteria>
- SOUL.md: n8n Boundary section present, Memory Architecture updated, ACIP block untouched
- TOOLS.md: Role boundary blockquote at top of n8n section, escalation section updated
- AGENTS.md: Operator Domain section appended at end of file
- docs/reference/templates/HEARTBEAT.md: Correct periodic check template, no pipeline monitoring
</success_criteria>

<output>
After completion, create `.planning/phases/09-agent-operating-model/09-01-SUMMARY.md`
</output>
