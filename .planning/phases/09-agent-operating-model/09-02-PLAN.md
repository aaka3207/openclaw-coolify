---
phase: 09-agent-operating-model
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - workspace/cron/memory-compaction.md
  - docs/reference/agents/main/lead-screening-icp.md
autonomous: true

must_haves:
  truths:
    - "AGENTS.md has a Memory Tiers and Compaction subsection documenting operational vs transaction tiers with 30-day retention rule"
    - "workspace/cron/memory-compaction.md exists with correct YAML frontmatter and instructs weekly digest production"
    - "docs/reference/agents/main/lead-screening-icp.md exists with ICP criteria for the lead screening workflow"
  artifacts:
    - path: "AGENTS.md"
      provides: "Memory Tiers and Compaction subsection inside Memory section"
      contains: "Transaction tier"
    - path: "workspace/cron/memory-compaction.md"
      provides: "Weekly memory compaction cron spec"
      contains: "memory-compaction"
    - path: "docs/reference/agents/main/lead-screening-icp.md"
      provides: "Lead screening ICP criteria document"
      contains: "ICP"
  key_links:
    - from: "workspace/cron/memory-compaction.md"
      to: "memory/digests/"
      via: "cron produces weekly digest files"
      pattern: "memory/digests"
    - from: "docs/reference/agents/main/lead-screening-icp.md"
      to: "leads/"
      via: "ICP defines criteria for what gets written to leads/ pipeline output"
      pattern: "leads"
---

<objective>
Encode memory discipline into AGENTS.md and create the two missing artifacts: a weekly memory compaction cron and a first-party lead screening ICP document. These establish the operational/transaction tier separation and give the agent clean input criteria for lead qualification.

Purpose: Without explicit compaction rules the agent stopped writing memory logs (Phase 8 drift). Without an ICP document the agent must infer qualification criteria each call.
Output: AGENTS.md memory section updated, memory-compaction.md cron created, lead-screening-icp.md created.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-agent-operating-model/09-CONTEXT.md
@.planning/phases/09-agent-operating-model/09-RESEARCH.md
@workspace/cron/weekly-retrospective.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Memory Tiers and Compaction subsection to AGENTS.md</name>
  <files>AGENTS.md</files>
  <action>
Read AGENTS.md in full. The Memory section begins around line 20 with "## Memory" and contains: daily notes, long-term MEMORY.md, MEMORY.md security rules, write-it-down guidance.

Insert a new subsection "### Memory Tiers and Compaction" after the existing "### What NOT to Memorize" block (which is already in the Memory Search Protocol section further down). Because the Memory Search Protocol section (lines 235+) is after a `---` separator, insert this new subsection into the original Memory section (lines 20-50), directly after the "### 📝 Write It Down" subsection and before the "## Safety" section.

Specifically, after the line "- **Text > Brain** 📝" (end of Write It Down section) and before "## Safety", insert:

```markdown

### Memory Tiers and Compaction

**Operational tier (permanent — keep forever):**
- `memory/YYYY-MM-DD.md` — daily narrative logs; raw record of what happened
- `memory/digests/YYYY-WXX.md` — weekly digests produced by compaction cron
- `MEMORY.md` — curated long-term memory (main session only)

**Transaction tier (30-day rotation):**
- `leads/` — lead screening pipeline output
- `monitor.log` — sandbox health monitoring log
- `recovery.log` — sandbox recovery log
- Clean these up periodically. A compaction cron handles routine cleanup automatically.

**Weekly compaction (cron-managed, runs Sunday 6 AM):**
1. Read past week's daily logs in `memory/`
2. Distill key events, decisions, and learnings into `memory/digests/YYYY-WXX.md`
3. Leave daily logs intact — they are the permanent audit trail

You do not trigger compaction manually. The cron handles it. If you want to compact early, do it inline: read daily logs → write digest → continue.
```
  </action>
  <verify>
Run: `grep -n "Memory Tiers and Compaction" AGENTS.md` — must find the subsection.
Run: `grep -n "Transaction tier" AGENTS.md` — must find the 30-day rotation tier.
Run: `grep -n "## Safety" AGENTS.md` — must still exist (section not deleted).
  </verify>
  <done>
AGENTS.md has Memory Tiers and Compaction subsection with operational/transaction tier definitions and weekly compaction description. Safety section still present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create memory-compaction.md weekly cron file</name>
  <files>workspace/cron/memory-compaction.md</files>
  <action>
Read workspace/cron/weekly-retrospective.md first — use it as the format template.

Create workspace/cron/memory-compaction.md with this exact content:

```markdown
---
# NOTE: Phase 9 created this file using the same format as weekly-retrospective.md.
# If weekly-retrospective.md auto-fires via workspace cron/ detection, this will too.
# If not (cron requires openclaw.json config), add a jq patch to bootstrap.sh for this schedule.
schedule: "0 6 * * 0"
agent: main
description: "Weekly memory compaction — distill past week's daily logs into memory/digests/"
---

# Weekly Memory Compaction

You are performing the weekly memory compaction. Distill this week's operational logs into a digest.

## Steps

1. List files in `memory/` matching `YYYY-MM-DD.md` from the past 7 days
2. Read each daily log from the past week
3. Identify: key decisions made, problems solved, patterns learned, user preferences noted
4. Write a concise digest to `memory/digests/YYYY-WXX.md` (create `memory/digests/` if needed):
   - Use ISO week format: e.g., `memory/digests/2026-W10.md`
   - 3-7 bullet points per category: Decisions, Problems Solved, Patterns, Preferences
   - Keep each bullet under 2 lines — this file grows over time
5. Do NOT delete or modify the daily log files — they are the permanent audit trail
6. Review `leads/` for files older than 30 days and delete them (transaction tier cleanup)

Keep entries brief. Future-you benefits from distilled signal, not raw noise.
```
  </action>
  <verify>
Run: `cat workspace/cron/memory-compaction.md` — file exists with YAML frontmatter and compaction steps.
Run: `grep "schedule" workspace/cron/memory-compaction.md` — must show `0 6 * * 0`.
Run: `grep "memory/digests" workspace/cron/memory-compaction.md` — must find the digest path.
  </verify>
  <done>
workspace/cron/memory-compaction.md exists with schedule "0 6 * * 0", agent: main, and 6-step compaction instructions. Uses same format as weekly-retrospective.md.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create lead-screening-icp.md ICP document</name>
  <files>docs/reference/agents/main/lead-screening-icp.md</files>
  <action>
Create the directory `docs/reference/agents/main/` if it does not exist.

Create docs/reference/agents/main/lead-screening-icp.md with the following content. This is behavioral config owned by Ameer (not the agent's operational output), so it lives in the repo and gets seeded to the main workspace. The ICP criteria come from what the agent self-organized around during Phase 8's lead screening pipeline:

```markdown
---
summary: "Ideal Customer Profile for lead screening"
read_when:
  - Screening leads from email newsletters
  - Classifying whether a company is a qualified prospect
---

# Lead Screening ICP

This document defines the Ideal Customer Profile (ICP) for outbound targeting. Use it when classifying leads from newsletters, job boards, LinkedIn, and other sources.

## Primary ICP: Series A / Series B Engineering-Led SaaS

**Company stage:** Seed (post-revenue) to Series B
**Headcount:** 15-150 employees
**Industry:** B2B SaaS, developer tools, AI/ML infrastructure, data platforms
**Business model:** Product-led growth (PLG) or sales-assisted PLG

## Strong Fit Signals

- Engineering team of 5-30 actively hiring (signals scaling pressure)
- CTO, VP Engineering, or Head of Engineering actively posting or hiring
- Pain signals in job descriptions: "scaling," "technical debt," "platform reliability," "developer experience"
- PLG motion evident: self-serve signup, usage-based pricing, or developer community
- Recently raised (Series A in last 12 months) — budget exists, growth pressure exists
- Building on modern stack (Next.js, TypeScript, Postgres, Supabase, Vercel, AWS)

## Weak Fit Signals (Deprioritize)

- Enterprise-only GTM with no self-serve component
- Non-technical founders with no in-house engineering
- Consulting/agency model (not a product company)
- Pre-revenue or pre-product (too early)
- Series C+ with 500+ employees (too big, long sales cycle)
- Government, healthcare, finance with heavy compliance requirements (slow)

## Disqualify If

- Competitor in the AI agent orchestration space
- Direct outreach already in progress (deduplicate against `leads/active.md`)
- Domain is obvious spam or lead gen farm

## Scoring Guide

When classifying a lead, output a score 1-5:

| Score | Meaning |
|-------|---------|
| 5 | Perfect fit — all strong signals, no weak signals |
| 4 | Good fit — most strong signals present |
| 3 | Possible fit — mixed signals, worth a second look |
| 2 | Weak fit — mostly weak signals |
| 1 | Disqualify — fails ICP or meets disqualify criteria |

Write qualified leads (score ≥ 3) to `leads/today.jsonl` using this format:
```json
{"company": "...", "url": "...", "signal": "...", "score": 4, "date": "YYYY-MM-DD"}
```

Review `leads/today.jsonl` at end of day. Anything score 4-5 gets surfaced to Ameer.
```
  </action>
  <verify>
Run: `ls docs/reference/agents/main/lead-screening-icp.md` — file exists.
Run: `grep "Score" docs/reference/agents/main/lead-screening-icp.md` — scoring guide exists.
Run: `grep "today.jsonl" docs/reference/agents/main/lead-screening-icp.md` — output format documented.
  </verify>
  <done>
docs/reference/agents/main/lead-screening-icp.md exists with ICP criteria, strong/weak fit signals, disqualify rules, 1-5 scoring guide, and output format for leads/today.jsonl.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -n "Memory Tiers and Compaction" AGENTS.md` — subsection exists
2. `grep -n "Transaction tier" AGENTS.md` — 30-day rotation tier documented
3. `cat workspace/cron/memory-compaction.md` — file exists with frontmatter and steps
4. `ls docs/reference/agents/main/lead-screening-icp.md` — file exists
5. `grep "today.jsonl" docs/reference/agents/main/lead-screening-icp.md` — output format present
6. `grep "schedule" workspace/cron/memory-compaction.md` — shows cron schedule
</verification>

<success_criteria>
- AGENTS.md Memory section has operational/transaction tier definitions and compaction protocol
- workspace/cron/memory-compaction.md exists with same format as weekly-retrospective.md
- docs/reference/agents/main/lead-screening-icp.md exists with ICP criteria and scoring guide
</success_criteria>

<output>
After completion, create `.planning/phases/09-agent-operating-model/09-02-SUMMARY.md`
</output>
